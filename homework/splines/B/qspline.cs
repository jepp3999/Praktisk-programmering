using System;
using Static System.Console;
using Static System.Math;

public class qspline{
	qinterp(double[] x, double[] y, double z){
		int i = binsearch(x,z);
		p
		double b[i] = 
		s[i] = y[i]  + b[i]*(z-x[i]) + c[i]*(z-x[i])*(z-x[i])
